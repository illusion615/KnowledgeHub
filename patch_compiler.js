const fs = require('fs');
const spec = JSON.parse(fs.readFileSync('./posts/agentic-sales-mobile-proposal/media/data-option-c.diagram.json', 'utf8'));
// I'll just check if there's a simple bug in the diagram spec.
