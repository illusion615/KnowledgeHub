{
  "id": "copilot-cli-json-timing-v1",
  "discoveredAt": "2026-08-25",
  "appliesPostHocTo": "copilot-model-benchmark-v1",
  "metrics": {
    "ttft": "model.call_start to first assistant.reasoning_delta, assistant.message_start, tool.execution_start, or tool.call_start event",
    "agentDuration": "first user.message to last assistant.turn_end",
    "processDuration": "CLI process spawn to exit"
  },
  "rationale": "VS Code Chat debug logs contain only session_start, but Copilot CLI JSON streaming exposes request, assistant, tool, and turn timestamps. Raw events permit the same post-hoc calculation for every run without changing prompts or model behavior."
}